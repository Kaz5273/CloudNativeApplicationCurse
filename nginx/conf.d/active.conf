# Couleur active : blue
# Ce fichier est reecrit automatiquement par scripts/deploy-blue-green.sh
# Ne pas modifier manuellement en production

upstream active_backend {
    server backend-blue:3000;
}

upstream active_frontend {
    server frontend-blue:80;
}
