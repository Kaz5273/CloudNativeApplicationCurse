# Copy to .env and fill in values before starting the monitoring stack
# cp .env.monitoring.example .env

# Grafana admin password (required – no default for security)
GRAFANA_ADMIN_PASSWORD=changeme_strong_password
